# Only needed when the frontend (Vercel) and backend (Render) are deployed
# separately. Point it at your Render service's public URL, no trailing slash.
# Leave unset for local dev (Vite proxies to FastAPI on :8000) or when
# FastAPI itself serves the built frontend.
VITE_API_URL=https://your-backend.onrender.com
