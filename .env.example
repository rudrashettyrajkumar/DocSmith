# No server-side keys required — this app is BYOK (bring your own key):
# users paste their OpenAI / Anthropic / Groq / OpenRouter key in the web UI
# and it is used for that single run only, never stored.
#
# Optional server tuning:
# LOG_LEVEL=INFO
